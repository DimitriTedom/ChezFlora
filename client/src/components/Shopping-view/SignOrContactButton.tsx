import { Button } from '../ui/button'

const SignOrContactButton = () => {
  return (
    <Button className="p-6 ombre bg-pink-400 hover:bg-pink-500 font-bold">Sign In</Button>)
}

export default SignOrContactButton